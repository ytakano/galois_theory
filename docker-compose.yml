services:
  rocq:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: >-
      sh -lc "if command -v rocq >/dev/null 2>&1; then rocq compile integer.v; else coqc integer.v; fi"
